import { LeaseDetailsPipe } from './lease-details.pipe';

describe('LeaseDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new LeaseDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
